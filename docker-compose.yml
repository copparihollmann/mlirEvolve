services:
  neo4j:
    # 1. UPDATE VERSION (Fixes vulnerability)
    image: neo4j:5.26.0  # Using latest stable 5.x is safest, or specifically 5.19.0
    container_name: mlir_neo4j
    
    ports:
      # 2. BIND TO LOCALHOST (Fixes security requirement)
      - "127.0.0.1:7474:7474" # HTTP Browser
      - "127.0.0.1:7687:7687" # Bolt Protocol
      
    volumes:
      - ./data/knowledge_base/neo4j/data:/data
      - ./data/knowledge_base/neo4j/logs:/logs
      - ./data/knowledge_base/neo4j/conf:/conf
      - ./data/knowledge_base/neo4j/plugins:/plugins
    environment:
      - NEO4J_AUTH=${NEO4J_USER}/${NEO4J_PASSWORD}
      - NEO4J_PLUGINS=["apoc", "graph-data-science"]
      - NEO4J_dbms_security_procedures_unrestricted=apoc.*,gds.*
      - NEO4J_dbms_memory_heap_initial__size=1G
      - NEO4J_dbms_memory_heap_max__size=2G
    restart: unless-stopped